
    {{>header}}
            <section class="login-sec d-flex justify-content-center align-items-center">
                <div class="container">
                    <div class="row">
                        <div class="col"></div>
                        <div class="col-5 p-5 rounded text-center login-form">
                            <h3 class="text-white ">{{success}}</h3>
                            <h2 class="text-white text-center form-heading">Login</h5>
                            <form action="/login" method="post" >
                                <input type="email" name="email" placeholder="Username/Email" class="mt-4 form-control p-2 bg light">
                                <input type="text" name="password" placeholder="Password" class="mt-4 form-control p-2">
                                <p class="text-danger mt-2 mb-0">{{login_err}}</p>
                                <p class="text-danger mt-2 mb-0">{{error}}</p>
                                <input type="submit" name="user_login" class=" btn mt-4 form-control w-50 btn-outline-light" value="Login">
                            </form>
                            <p class="mt-2">
                            <a href="" class="mt-3">Forgot Password!</a></p>
                            <a href="/register"><button class="btn btn-outline-secondary text-light  w-75 py-2 fs-5" >Create New Account</button></a>
                        </div>
                        <div class="col"></div> 
                    </div>
                </div>
            </section>
        </section>
    {{!-- {{>footer}} --}}